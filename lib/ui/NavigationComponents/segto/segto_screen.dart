import 'package:flutter/material.dart';
class SegtoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(
        children: <Widget>[
          Text('Seguimiento'),
        ],
      ),
    );
  }
}